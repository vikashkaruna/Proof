# W4.3 identity verification foundation — 22 September 2026

W4.2 core staging merge `46847d8` passed CI 35663034387, including exact-merge container parity. This next slice supplies identity verification and attestation evidence, not production acquisition authority.

The verifier follows the JWT-SVID signature/header/subject/audience requirements and adds explicit Axiom restrictions: required issuance time, bounded lifetime, single service audience and canonical unencoded paths. Trusted bundle selection uses only an allowlisted subject trust domain; no token-selected network discovery occurs. Public key material is validated, private/symmetric/non-signing keys are refused, and bundle freshness/revision is checked again after signature verification. Unknown custom claims are not returned as authorization. [JWT-SVID standard](https://github.com/spiffe/spiffe/blob/main/standards/JWT-SVID.md).

The registration adapter scopes its database query to tenant, verified subject and active status, then verifies exact agent and declared permission. Repeating calls observes disablement. This does not supply tenant-task delegation, estate/connector grants, kill-switch or action approvals; the broker's deny-all default is unchanged. Shared runtime credentials and unchecked direct tool/data paths remain W4.3 work.

Real acceptance uses SPIRE 1.15.3 binaries pinned by SHA256 for both supported Docker architectures and a digest-pinned base image. Server and node agent communicate only on loopback in a network-disabled disposable container, using an exported CA and one-time join-token file. Ten distinct UIDs receive distinct workload SVIDs; requesting another agent's identity or using an unknown UID fails. The BFF validates actual signatures and refuses audience/signature/bundle failures. No Docker socket or host PID mount is required. [SPIRE configuration](https://spiffe.io/docs/latest/deploying/spire_server/).

Review fixed host-bind SQLite initialization problems by retaining issuer state inside the container, public socket-directory permissions for registered non-root UIDs, and partial download cache handling. An exploratory diagnostic printed synthetic short-lived SVIDs; that issuer was removed and never trusted by the application. The reusable runner stores no token files and only emits fixed stage names or allowlisted boolean results; subprocess errors and identity payloads are never echoed. Fixture issuer state is destroyed during cleanup.

Scope/metadata corrections retain Sudhaar's non-mutating contract, preserve Nazar's previous fix, and add explicit Prativedan read declarations. Tests compare TS/Python scopes and mutation metadata. The architecture now distinguishes internal record changes from client mutations, without claiming declarations physically isolate workloads.

Local evidence: 42 identity/registration tests, 513 total BFF tests, 144 Python tests, 61 real SPIRE/BFF outcomes; workspace and dependency checks pass. Existing unrelated Python lint debt remains. The fixture tests no cloud deployment, tenant/task delegation, token exchange, client authorization server or client action. No migration was needed; tip remains 0040. Next complete W4.3 isolation/runtime enforcement and actor-chain work, then W4.4 grants and full W3 wizard/graph.

Additional runtime review fixed the mismatched internal-token environment name and the generic route's missing-token authentication bypass. Thirteen new configuration/HTTP checks bring the Python suite to 144. A configured matching key is now mandatory before agent lookup/invocation; this shared transport key still conveys no workload authority. Review also identified existing runtime audit fallback/raw-payload/completion-failure defects, recorded as blockers in Docs 11/15/16 for the next runtime isolation slice. The BFF browser suite does not cover those paths.
