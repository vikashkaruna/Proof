# Staging integration review — 21 September 2026

Reviewed `ea27df9` against the accepted roadmap and incorporated all ten upstream commits after `84c3b16`. This review preserves the founder's choices: self-hosted Supabase, fresh approval on redelivery, configured proxy trust, deferred email OTP, and no billable/irreversible deployment during this session.

| Finding                                                                   | Evidence                                                                                                                       | Correction                                                                                                                                                           |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Managed service authority was untested and unavailable without BYPASSRLS  | Strengthened real GoTrue/PostgREST rehearsal found a service token reading 0 known tenant rows                                 | 0024 adds policies only for the existing service principal, without widening client grants or ledger writes; positive read/write and negative client assertions pass |
| Unused old token survived reconciliation                                  | Existing 0023 test created its allegedly fresh token before release; added assertion failed with `issued` instead of `revoked` | 0025 revokes overlapping issued authority; consumed tokens stay consumed; a genuinely subsequent approval is required                                                |
| Reconciliation could release work without ledger proof                    | SQL release committed before the BFF's separate append                                                                         | 0025 includes `append_ledger` in the transaction; an injected ledger failure preserves claims/outbox/tokens                                                          |
| Execute omitted its matrix capability check                               | Route proceeded directly to kill/token checks despite `PLAN_EXECUTE` being restricted                                          | Explicit capability refusal precedes token processing; six role tests and real valid-token viewer denial pass                                                        |
| MFA limiter failures were misclassified                                   | Service returned `rate_limited`; issue route returned enrollment-required, verify route returned incorrect-code                | HTTP 429 with Retry-After; real issue/verify limit tests pass                                                                                                        |
| Preprod auto-confirmed identities to compensate for absent email delivery | Terraform explicitly configured weaker preprod behavior                                                                        | Auto-confirm off, public registration disabled until verified delivery; real admin-provisioned password login still passes                                           |
| Runtime database URLs lacked the migration path's TLS floor               | Secret Manager URL templates omitted sslmode                                                                                   | Both generic and GoTrue runtime URLs now require TLS; no deployment performed                                                                                        |

The upstream action digest, proxy parsing/budgets, deprecated RPC removal and deployment fail-closed changes are retained. Content binding is verified with real database edits between challenge and approval. This is not proof of atomic approval issuance: action reads, challenge consumption, token persistence, action update and ledger append still span transactions. That remains the next W1 safety slice, with race/fault injection required. Dry-run/action snapshots also need enforcement at the executor, which is still a refusal stub.

The self-hosted rehearsal uses a Supabase PostgreSQL image for required extensions but creates its database from template0 and explicitly removes service-role BYPASSRLS. It does **not** prove all Cloud SQL administrative permissions, network/IAM or provider behavior. [Cloud SQL's documented role attributes](https://docs.cloud.google.com/sql/docs/postgres/users) differ from a full PostgreSQL superuser. A reviewed deployed smoke test remains required.

Supabase Storage still has only bootstrap schema contracts, not a deployed storage API. GCS evidence sealing remains fail-closed until a native verified lock adapter exists. The deployment bootstrap, role separation, confirmation/invitation path, gateway/ingress and TLS identity validation deserve live acceptance before any W0 completion claim. No cloud apply, billing action, live client mutation or irreversible bucket lock was performed.

Verification commands/results and the resume sequence are recorded in Docs 14 and 15. Earlier milestone counts are historical; do not infer whole-workstream completion from green CI or those counts.
