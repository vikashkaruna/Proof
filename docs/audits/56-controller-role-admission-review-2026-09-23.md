# Review 56 — controller workload role admission

The current composition read authenticated JWT bundles at startup, but never proved that the calling process held the controller identity. A registered worker can legitimately fetch the same public trust material. The missing check is closed before backend policy access; the node's protected socket and reviewed exact node/UID/image registration remain trusted infrastructure.

The fixed unary FetchJWTSVID descriptor follows the [SPIFFE Workload API protocol](https://github.com/spiffe/spiffe/blob/main/standards/workloadapi.proto). It requests only the controller SPIFFE ID and one dedicated startup audience, accepts exactly one bounded response and independently verifies its signature/claims/current trust with the existing verifier. The node-health wrapper still enforces issuer freshness. No bearer leaves the function, and errors are fixed strings. There is no fallback, new environment setting, application scope or mutation permission.

Real Unix gRPC tests exercise transport metadata/request fields and adversarial claims, response shape, signature, expiry, deadline and trust change. Composition tests require no backend access on admission refusal. Real local SPIRE acceptance contrasts correct controller admission with a registered worker that can read bundles but cannot obtain the controller identity; actual entrypoint acceptance remains in the full worker gate. Test attestation is local join-token, not GCP. Production registration must retain exact node/UID/image matching and a JWT-SVID TTL no greater than 300 seconds.

Validation counts and exact staging merge evidence are saved in the session after checks complete. Revision 66 CI initially failed Supabase startup in the persona job before running browser tests; it must not be represented as green until the applicable jobs pass. No cloud resources were provisioned. This closes a startup check, not host delivery, continuous controller role reattestation or the broader roadmap.
