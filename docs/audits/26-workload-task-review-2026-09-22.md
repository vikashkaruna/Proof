# W4.3 task delegation review — 22 September 2026

The preceding staging merge `e46c1b7` is green in CI 35678105419 (17 applicable jobs). Six sanitized artifacts were checked for that exact SHA: 61 identity outcomes, five runtime-audit outcomes and 63 browser/89 API outcomes per configuration. No intervening upstream implementation was present. Service IAM engineering is complete; effective cloud permission checks remain pending.

A valid SVID establishes the workload identity, not the human delegation or the tenant task it may access. Agent-run UUIDs are visible to authorized readers and cannot serve as bearer authority. The new component requires both an authenticated workload and an independent 256-bit random task proof, stored only as SHA-256. SQL rows bind the proof to the exact tenant/run/agent/workload, initiator, optional estate/engagement, input digest, scopes and deadline. Five minutes is the BFF default; SQL caps issuance at fifteen minutes. Tool authorization expires at the earlier of the task and SVID deadlines.

Migration 0041 supplies backend-only SELECT and service-only SECURITY DEFINER RPCs with an empty search path. Direct service-role insert/update/delete is refused. Run, delegation and append-only audit commit together. The audit contains context identifiers/scopes/deadline, never proof material or raw inputs. Browser/anonymous roles cannot read task hashes or call the RPCs. Existing runs retain their values and receive no delegated authority.

Current initiator membership, internal-user flag where needed, active registration, active estate, engagement assignment, run status/input/correlation, expiry, revocation and global/tenant halt state are checked on lookup. Membership/parent/workload locks protect issuance against concurrent parent changes; revocation locks the task and appends exactly one event. Both issuance/demotion orderings are tested. A later demotion removes authority even if issuance committed first. Audit failure rolls back both issuance and revocation. Admins can manage tenant tasks; analysts can revoke only their own. Generic Karya issuance is refused until a reviewed execution-specific path exists.

BFF tests exercise canonical proof parsing, random proof uniqueness, hash-only persistence, redacted serialization, input copying before awaits, server-selected scope, current store lookup on every invocation, all context mismatches, SVID/task expiry including expiry during lookup, malformed responses and sanitized errors. There are 33 new BFF tests, 546 total. Real PostgreSQL tests cover permissions, state changes, concurrent locks, audit failure and upgrade preservation; all 42 migrations and the full database suite passed. Workspace tests/lint/typecheck also passed.

The proof is not a connector credential, an approval or an actor-chain token. SQL trusts the BFF issuer to select contract scopes and derive the session/input context; it does not verify a JWT. The component has no application HTTP route yet and no worker receives its proof implicitly. Actual tool handlers must consume its immutable context and repeat checks at the mutation boundary. A lookup cannot hold a database lock across external side effects. Issuance is not request-idempotent by itself; the future controller must enforce that separately. A lost proof requires revocation and new issuance, not hash disclosure.

Remaining W4.3: remove shared backend/approval/storage credentials from agent workers, implement production trust delivery and registration lifecycle, bind private orchestration to task authority, enforce tenant/estate filtering in every actual tool and verify token-exchange actor chains. W4.4 must supply live grants, approval/dry-run/rollback and controlled target invocation before broker activation. Full W3 onboarding/readiness and graph remain next, followed by W4.5/6/7. None of these are closed by this component. Exact committed-container and merge CI results are recorded separately after execution.
