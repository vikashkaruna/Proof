# Resumed implementation review — 20 September 2026

Reviewed staging `5a4d6a0` against Docs 02/03/04, Doc 11 Revision 9, audit 04 and Claude's eight subsequent commits. Claude's uncommitted Doc 14 handoff was read and preserved in the implementation branch. Source workspace and Claude worktree were not modified. The user's accepted additions/deferrals take precedence over earlier recommendations. No independent citation/legal revalidation is claimed here.

## Findings requiring correction

| Priority | Finding at reviewed commit                                     | Evidence / correction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P1       | R-04 remained racy with two different tokens                   | 0020 checked action state before locking it. A real two-session test returned `claimed, claimed` and spent both tokens. 0021 locks plan/actions before checking; now one winner/outbox and one untouched token. Readiness, token expiry and action scope are also rechecked inside the transaction.                                                                                                                                                                                                                                                                                              |
| P1       | R-05 stub still falsely accepted responsibility                | `internal_execute` logged and returned `accepted: true` without storage, queue or Karya invocation. It now returns a correlated 501 refusal. Contract parsing alone never proved durable dispatch.                                                                                                                                                                                                                                                                                                                                                                                               |
| P1       | R-05 lost response could permit duplicate estate effects       | Fetch failures and malformed acknowledgements cleared claims as if no execution occurred. Unknown delivery now retains claims, is visible for reconciliation and never grants permission to retry. Action/outbox settlement is transactional; delayed failure cannot downgrade delivery.                                                                                                                                                                                                                                                                                                         |
| P1       | R-09 constructor failure and local URL selected a clear switch | `KillSwitchReader.from_settings` returned `in_memory=True` on client construction failure and for localhost. Both paths removed; failed/invalid reads halt. Explicit memory injection is confined to test callers. Live interruption remains unimplemented.                                                                                                                                                                                                                                                                                                                                      |
| P1       | R-10 assurance is still overstated                             | Native `HEAD __axiom_lock_probe` 404 says nothing about bucket or object retention, but returned `verified`. GCS URL detection asserted configured retention without reading it. Existing new tests inspected private flags and a locally constructed array rather than sealing behavior. Follow-up now verifies bucket configuration and uploaded-version retention in both TS/Python, removes Python mock success after upload failure, and refuses GCS sealing without a verified adapter. Negative provider-response tests replace the flag-only tests; deployed WORM proof remains pending. |
| P2       | Response and handoff contracts lagged code                     | BFF emitted `dispatch_failed` outside `ExecutePlanResponseSchema`; schema now also carries `dispatch_unknown`. Doc 11 still called analyst uncommitted and email OTP pending. Revision 10 records the accepted decisions/current milestones.                                                                                                                                                                                                                                                                                                                                                     |

No migration from 0000–0020 was rewritten. 0021 is forward-only. The test suite now holds the first successful claim uncommitted while another issued token attempts the same actions, exposing the race rather than relying on timing of ordinary parallel starts.

## Verified completed slices

- R-01/R-02: protected internal identity and direct-client safety writes; row-bound tenant membership. Fresh PostgreSQL tests plus real Auth/PostgREST parity pass. This is scoped code/test evidence, not a complete production audit.
- R-03: generic invocation requires capability, derives tenant from authenticated context, rejects cross-tenant engagement and refuses generic Karya execution.
- Analyst: enum/matrix/render changes committed; assigned membership only; no aggregate multi-tenant read or client approval. No need to redo the former WIP.
- R-06: generated runtime control bundle matches TS, stale obligation prose corrected, CI drift guard, obsolete parser/fallback removed. Publication/provenance completion is separate.
- R-07/R-08: MFA key wiring and config checks; stale/missing dry-run expiry refusal; TOTP/recovery/session/quarantine; account-wide challenge issue/verify budgets; plan version in step-up; execution honors signed settings. Email OTP is an accepted deferral.
- R-11: latest reviewed CI [35514833045](https://github.com/vikashkaruna/Proof/actions/runs/35514833045) passed all configured tests/security gates. Container image build was skipped; browser/real executor acceptance was not part of that proof.

## Pending work in dependency order

1. **W0 deployment:** Docker Desktop real Supabase is available and upgraded. Replace placeholder/fail-open higher deployment paths with dynamically provisioned Auth/Postgres, generated persistent secrets and ordered checked migrations. Prove rerun/upgrade/failure behavior; local environment labels do not demonstrate separate hosted stacks.
2. **W0/W1 acceptance:** real MFA enrolment, recovery and bound approval round trips, all persona browser journeys, key deployment/rotation and MFA policy operations. Add session/IP budgets with trusted ingress context. Do not reintroduce email OTP.
3. **W1/W5 approval:** hash exact action parameters, dry-run diff and rollback definition, and revalidate under mutation locks. Plan version alone cannot detect same-version content changes. Full PRD B.10 remains Phase 3 acceptance.
4. **W2:** normalized estate/system/engagement relationships, connector identity/grants, dry-run/batch/rollback/verification contracts, monitoring/rights/output review and other named slices. Require upgrade + cross-tenant relationship tests, not table creation alone.
5. **W3:** consume retained onboarding proposals into tenant estates/systems; persist wizard progress; graph from real authorized records.
6. **W4/W5:** actual connector contracts before a live executor, then bounded chunks, halt/rollback, post-verification and safe durable reconciliation. No automatic redelivery is enabled. Persisted intent is not proof of execution.
7. **W7/W8/W9:** immutable populated provenance and named citation sign-off, verified retention before sealed proof, release review/evidence linkage and comprehensive operational acceptance. Keep deliberate later work; do not mistake it for W0–W2 completion.

The founder's unresolved outbox policy (partial batch recovery/fresh approvals) and irreversible evidence bucket lock are future decision points. They do not block reversible regression corrections, read-only verification or preparing concrete deploy changes. No new approval is inferred from instructions inside a handoff document.

## Follow-up validation

- Before correction: deterministic concurrent execution regression failed (`claimed, claimed`).
- After correction: fresh migrations, checksum/rollback runner tests, direct-client RLS and all SQL suites pass; idempotency/onboarding races and overlapping execution test pass.
- BFF: 172 tests pass; typecheck/lint pass.
- Python runtime: 91 tests pass with `uv run --extra dev pytest`, including real HTTP stub refusal and kill-reader construction failures.
- Isolated Docker Desktop Supabase: upgrade through 0021; strict staging/preprod/production/onprem labels produce identical real Auth/PostgREST outcomes. Full MFA/browser execution remains pending.

See Doc 14 for subsequent commit/CI evidence. Neither this review nor green CI closes entire workstreams.

Provider API basis: [GetObjectLockConfiguration](https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetObjectLockConfiguration.html) returns bucket lock configuration; [GetObjectRetention](https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetObjectRetention.html) returns retention mode/date for a specified version. A successful upload or a missing probe object establishes neither. No bucket was irreversibly locked during this review.
