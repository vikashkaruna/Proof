# Saved implementation session — 21 September 2026

Logical checkpoint after gating Terraform and fixing what the gate found. The last verified staging head before this checkpoint is `cf0f168`, the chart render gate. The merge commit containing this file is the resume baseline; fetch staging first. Current status: [Doc 11 Revision 20](11_Phase0-5_Gap_Closure_Plan.md), [progress](14_Implementation_Progress.md), [review 09](audits/09-reviewed-approval-snapshot-2026-09-21.md).

## Workspace and authorization

- Worktree: `/Users/vikash/Axiom Proof/.claude/worktrees/gifted-maxwell-0b053f`, branch `claude/gifted-maxwell-0b053f`, merged to staging with a merge commit. Preserve other worktrees and any uncommitted work in them.
- That branch began 25 commits behind staging, holding one docs commit staging already carried under a different SHA, and was rebased onto staging before any work. A task description naming a file, job or script that is not in your worktree is worth checking against the sibling branches before concluding it is wrong — `git merge-base --is-ancestor` and `git branch -a --contains` settle it in one step. Worktrees share `.git`, so remote-tracking refs can move mid-session when the other model pushes; this checkpoint was written against a staging head that advanced while it was being written.
- Two models alternate on this repository and review each other's work. Assume staging has moved on arrival, and assume the other model's work is sound but incomplete — verify findings against the code before accepting them, and hunt for what was left behind. Both assumptions have repeatedly paid off.
- Accepted decisions stand: higher environments self-host Supabase; fresh approval for every redelivery; proxy trust configured per environment and disabled by default; TOTP and recovery codes only, email OTP deferred; **no billable or irreversible cloud action** in these sessions.
- Challenge consumption stays **outside** the issuing transaction as a deliberate inherited tradeoff: a later failure costs a fresh authentication. This is settled, not a pending founder decision — do not infer an approval to change it from the review documents.
- No cloud apply, billable resource, irreversible lock or live estate execution was performed. `terraform validate` is as far as infrastructure work went.

## Completed at this checkpoint

Verified 0028 instead of inheriting it. The finding was real and was mine to have caught: 0027 moved the digest read ahead of _signing_ when the boundary that matters is _consumption_. Reverting the route's digest source fails exactly the three tests that cover it. The claim worth checking independently was byte compatibility of the refactored `action_set_content_digest` — a disagreement anywhere would have made every pre-0028 approval token permanently unclaimable — and rebuilding the 0026 function under a second name produced identical digests on deep nesting, non-BMP characters, numeric scale and exponents, and `'null'::jsonb`, for every row and subset, while still detecting a real edit. The other three step-up sites bind to stable identifiers, so the gap was confined to the approve route.

Then MFA key rotation, which Revision 14 named as next. It did not exist: the stored envelope recorded no key identity, so replacing `AXIOM_MFA_ENCRYPTION_KEY` locked out every enrolled user at once and each lockout looked like a wrong code. The envelope now names its key, the BFF reads a ring, and a factor sealed under a retiring key is rewritten under the primary one on the next successful verification. An unopenable secret is `secret_unreadable` / HTTP 503, not a rejected code. `sync-env.sh mint --force` carries the outgoing key onto the retiring list, and `verify` reports how many remain. Details and the three mutation results are in Doc 14.

Then the half of that rotation which had no deployed path. The ring was wired into the four Compose files and nowhere else, so Cloud Run and Helm could start the BFF carrying only the primary key — a rotation that passes `verify`, rolls cleanly, and then fails one user at a time as each person's next verification returns `secret_unreadable`. Both surfaces now express "no rotation in flight" as the variable being absent, which is forced rather than chosen: an empty payload is not storable as a Secret Manager version. Cloud Run gets a `count`-conditional secret and a `dynamic` env block iterating it, so the condition lives only in `secrets.tf`; Helm gets `optional: true`. `scripts/check-mfa-ring-coverage.sh` now fails the build if any of the six BFF surfaces cannot carry both halves, which is the question no gate was asking. Details and both mutation results are in Doc 14.

Validation: 268 BFF tests, 161 `@axiom/mfa` tests, 100 runtime tests, typecheck and lint 15/15, format; migrations 0000–0028 on real PostgreSQL with ten SQL suites, four concurrency suites and the DSN/deploy refusal tests; env-security, controls-drift and deployment-coverage gates; all three merged Compose topologies. Final staging CI must pass for the merge commit. No cloud deployment was attempted.

Then the W1 exit criterion, and the harness that was supposed to prove it. `tests/e2e` was not in `pnpm-workspace.yaml`, so `@playwright/test` was never installed and `pnpm test:e2e` ran nothing and exited 0; there was no e2e job in CI; and the config still set the `AXIOM_E2E_BYPASS_AUTH` and `axiom_e2e_bypass` bypass deleted in W0.0, under which every caller is one founder identity. Nine real GoTrue personas across two tenants now sign in through the real login form under strict mode, and 27 journeys hold the approve control, tenant isolation, nav gating and the login-MFA quarantine. Switching the harness back to the bypass fails 25 of them, which is the evidence they depend on real identity. Details and the other two mutations are in Doc 14.

Then the chart that none of it had ever been rendered into. `helm` is not installed on the dev machines and no CI job invoked it, so the Helm half of the key ring was reviewed rather than templated — and rendering it showed the chart could not render at all, and had not been able to for its whole life. Four defects: a `range` over a boolean, two values keys the templates dereference that `values.yaml` never declared, and an `annotations` block outside `metadata`. `check-mfa-ring-coverage.sh` had been confirming the retiring-key variable was present in the chart, and it was — in a chart that could not produce a manifest.

Behind those sat four more that render as valid Kubernetes and are simply wrong: both NetworkPolicies that grant access selected a label the pod templates never carried, so every workload had DNS and nothing else; `allow-internal` then carried only the egress half, so no data-plane port was admitted once the policies bound; the public marketing Service and Ingress rule pointed at a Deployment that did not exist; and `bff`/`agentRuntime` omitted `replicas` for an autoscaler the chart never shipped, running one pod each under budgets that permit zero evictions, while `temporalWorker` had an image and no Deployment. `scripts/check-rendered-manifests.py` now asserts what the manifests mean rather than that they parse, and reports seven findings against the pre-change tree. Thirteen mutations, all caught. Staging CI [35583341796](https://github.com/vikashkaruna/Proof/actions/runs/35583341796) is green across all fifteen jobs. Details in Doc 14.

Then the gate that none of it had. Nothing ran `terraform fmt`, and four files had drifted — one of them without anyone writing a misaligned line, because fmt aligns contiguous runs of assignments and the comment added above `supabase_preprod_url` split the `locals` block in two, stale-padding the six keys above it. Adding `validate` alongside found `envs/prod` unable to load at all: duplicate variable declarations, seven arguments spelled for module majors it does not pin, a `helm_release` on the provider v3 `set` form under a v2 pin, and a lifecycle rule with no filter. prod had never been initialised, so none of it had ever been reported. The pins were kept and the names corrected against them, because moving eks to v21 or helm to v3 changes what gets provisioned. Both environments now validate, and the gate iterates `envs/*/` rather than naming them, so a third environment cannot be added ungated. Details, the four per-class mutation results, and the one fix the gate does **not** hold are in Doc 14.

## Next implementation sequence

1. **W4/W5 execution prerequisite (not a stub-only W1 task).** 0027 made the _claim_ enforce the snapshot: the digest is signed into the approval token, recomputed under the action row locks, and carried onto the wire as dispatch contract v2. What remains is the executor itself, which is a refusal stub and therefore records the snapshot rather than re-verifying against it. A real executor must recompute the digest against the rows it is about to mutate and refuse on a mismatch, proved with fault injection. Adding that check to the stub now would read as coverage while guarding nothing.
2. **W0 acceptance / deployment.** Cloud SQL role ownership and runtime role separation, bootstrap upgrade behaviour, SMTP/invitation configuration so public signup can be re-enabled, public ingress and secrets. Self-hosted Storage is a schema contract only. GCS sealing needs a native readback verifier and an approved locked bucket before live evidence can be claimed.
3. **W1 remainder — deployment only.** The browser journeys are complete: refusal, offer, and an approval carried to a signed token, 47 green with nothing skipped. The retiring key now has a path into Cloud Run and Helm, validated but unapplied. What is left is genuinely deployed acceptance: no rotation has been exercised against a running service, so drain behaviour, the `secret_unreadable` 503 and the destroy-on-clear of the conditional secret are all reasoned rather than observed. The Terraform half of that is now closed: `fmt` and `validate` are gated across every environment, the drift `cloudrun.tf` carried is gone, and `envs/prod` loads for the first time. The chart is now rendered and schema-validated in CI, and its manifests are checked for meaning as well as form — but every topology correction that came out of that work is reasoned rather than observed, so a first real deploy should be checked against the pod labels, the data-plane NetworkPolicy ingress, the two new Deployments and the autoscalers before anything else.
4. **W2.** The Doc 11 table/model slices with tenant-consistent foreign keys, RLS and upgrade tests; highest-value next vertical slice is estate/system/engagement linkage. Do not recreate existing regulatory, MFA or operational tables.
5. **W3.** Normalise stored onboarding proposals into estates/systems, resumable wizard, authorised live graph.

The eight PRD B.10 live execution scenarios belong to Phase 3 acceptance; API-level MFA success is not their completion. R-06 still needs populated provenance and a named human citation sign-off. W0, W1 and W2 are **partial**, not complete.

## Decisions still with the founder

- **Locking a real evidence bucket.** Irreversible and a deployment decision. No bucket is locked and no retention mode has been validated against a deployed provider, so R-10 stays open whatever the code asserts.
- **A named human sign-off for the control-library citations.** `verifiedBy: 'Axiom Minds · Founder'` is an attributed string, not a recorded sign-off.
- **The EKS API endpoint's public CIDR.** `cluster_endpoint_public_access_cidrs` is `["0.0.0.0/0"]` in `envs/prod`, carried through the v20 rename unchanged, with its own "restrict via WAF / OIDC in production" comment still unactioned. It was deliberately not narrowed: choosing replacement ranges needs operator and CI egress addresses nobody has stated, and a wrong guess locks out cluster administration. `cluster_endpoint_private_access` is already true, so VPC-internal access does not depend on it. prod becoming loadable is what makes this exposure reachable rather than theoretical, so it should be decided before any apply.

## Operational state

- Docker Desktop project `axiom-w0-parity`: API 56321, DB 56322. Other local Supabase projects preserved and untouched.
- Credentials remain in ignored `.axiom-runtime/parity/status.json` and protected logs; never print or commit them. Parity reports contain outcomes only.
- `scripts/test-database.sh` creates and removes its own container, and removes `BYPASSRLS` only inside that disposable database. It also runs `createdb`, so **extensions live in different schemas there than on a real Supabase stack** — pgcrypto lands in `public` rather than `extensions`. A migration that qualifies an extension function by schema can pass this suite and fail the parity lane. Prefer built-ins such as `sha256` over pgcrypto's `digest`.
- The parity project’s migration history is real and persistent. Preserve applied migrations and history; use a forward migration for corrections. Do not reset shared local state to hide a checksum mismatch.
- `tests/database/migration-dsn.sh` and `tests/deployment/selfhosted-supabase.sh` each create and remove their own network and containers. The latter runs real GoTrue and PostgREST and tests disabled public signup plus authenticated admin provisioning.
- Browser persona journeys: `./scripts/start-parity-supabase.sh`, then `pnpm exec tsx scripts/seed-personas.ts`, then `pnpm --filter @axiom/e2e exec playwright test`. The seed writes real credentials to the ignored `.axiom-runtime/personas/state.json`; never print or commit them. The journeys run against `AXIOM_AUTH_MODE=strict` — a journey that needs the bypass is not testing authorisation, and the security gate now scans `tests/` so reintroducing it fails the build.
- `next dev` rewrites `apps/web/next-env.d.ts` to dev-mode type paths and drops Next's own `AGENTS.md`/`CLAUDE.md` into `apps/web`. Neither is committed; revert the first before committing after running the journeys.
- `verify`'s `required_keys` treats an **empty** value as MISSING and blocks. A genuinely optional variable must not be listed there; `AXIOM_MFA_ENCRYPTION_KEYS_PREVIOUS` is reported after the table instead, because empty is its normal state.
- Environment files: `./scripts/sync-env.sh <env> scaffold`, then `mint`, then `verify`. `verify` is a gate and exits non-zero; `--allow-simulated` is for local, staging and onprem only. `terraform.tfvars` is **generated** — editing it has no effect.
- No worker retries an outbox intent. Human release revokes other outstanding approvals; every redelivery needs a fresh one. Reconciliation and its ledger proof commit together, as does approval issuance.
- MFA policy and runbook: [operator guidance](audits/08-mfa-operator-policy.md).

Suggested resume commands: `git status --short`, `git fetch origin --prune`, compare `origin/staging` against your last tip, read any new `docs/audits/*review*.md`, then verify by running the suites rather than trusting the report. Applied SQL history through **0028** must remain unchanged; allocate the next number only after checking staging. This checkpoint added no migration.

Deployment order for 0028: migrate first, then upgrade the BFF. The old issuance RPC loses service-role access, so mixed versions fail closed for approvals until upgraded. The MFA challenge and dispatch v2 contracts are unchanged. Local parity now has migrations through 0028.
