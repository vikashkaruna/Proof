# Generated reference-prototype bundles; these are rebuilt by dc-runtime.
Axiom Proof App/design/support.js
Axiom Proof Handoff Map/support.js
Axiom Proof Site/design/support.js
design-system/support.js
infra/helm/
pnpm-lock.yaml

# Generated control library JSON; written by `pnpm tsx scripts/build-controls-json.mjs`
# Its 2-space JSON.stringify output doesn't match prettier's exact formatting and would
# re-break `pnpm format:check` on every regen. Source of truth is the TS module.
services/agent-runtime/src/axiom/data/controls.json

# Next.js auto-generated type definitions
**/next-env.d.ts

# Firebase CLI cache & build outputs
.firebase/


# Hand-authored reference documents.
#
# Prettier's markdown rules (emphasis style, table-column padding) rewrite
# these wholesale without changing a word of content, which buries real edits
# in reformatting noise and makes the plan documents hard to review. They are
# prose, not code, so they are excluded rather than reformatted.
docs/11_Phase0-5_Gap_Closure_Plan.md
Axiom-Proof_Readiness_Matrix.md
VK_QUICK_REF_PROOF_MODULES_SETUP.md
