# Forces the `create: false` path, where the chart binds the deployments to a
# ServiceAccount it does not manage. Renders no ServiceAccount object, so it
# also proves the deployments still resolve a name without one.
serviceAccount:
  create: false
  name: axiom-proof-preprovisioned
